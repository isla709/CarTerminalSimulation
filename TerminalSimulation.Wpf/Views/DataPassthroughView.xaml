<UserControl x:Class="TerminalSimulation.Wpf.Views.DataPassthroughView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
 <Grid Background="{DynamicResource MaterialDesignPaper}">
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <Border Grid.Row="0" Background="{DynamicResource MaterialDesignCardBackground}" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1" Padding="8">
   <WrapPanel VerticalAlignment="Center">
    <TextBlock Text="串口:" VerticalAlignment="Center" Margin="0,0,4,4"/><ComboBox ItemsSource="{Binding SerialPorts}" SelectedItem="{Binding SelectedSerialPort}" Width="90" Margin="0,0,8,4"/>
    <TextBlock Text="波特率:" VerticalAlignment="Center" Margin="0,0,4,4"/><ComboBox ItemsSource="{Binding BaudRates}" SelectedItem="{Binding SelectedBaudRate}" Width="85" Margin="0,0,8,4"/>
    <Button Command="{Binding ToggleSerialPortCommand}" Content="{Binding SerialPortBtnText}" Style="{StaticResource MaterialDesignOutlinedButton}" Height="28" Padding="12,0" Margin="4,0,8,4"/>
    <Button Command="{Binding RefreshSerialPortsCommand}" ToolTip="刷新串口" Style="{StaticResource MaterialDesignToolButton}" Height="28" Width="28" Padding="0" Margin="0,0,12,4"><materialDesign:PackIcon Kind="Refresh" Width="16" Height="16"/></Button>
    <TextBlock Text="聊天框显示编码:" VerticalAlignment="Center" Margin="0,0,4,4"/><ComboBox SelectedIndex="{Binding ChatEncodingIndex}" Width="85" Margin="0,0,12,4"><ComboBoxItem Content="GBK"/><ComboBoxItem Content="UTF-8"/><ComboBoxItem Content="HEX"/></ComboBox>
   </WrapPanel>
  </Border>
  <ListBox Grid.Row="1" ItemsSource="{Binding PassthroughMessages}" Margin="8" Background="Transparent" BorderThickness="0"
           VirtualizingStackPanel.IsVirtualizing="True" VirtualizingStackPanel.VirtualizationMode="Recycling" ScrollViewer.CanContentScroll="True">
   <ListBox.ItemTemplate><DataTemplate><Grid Margin="0,4"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
    <StackPanel Grid.Column="0" HorizontalAlignment="Left" Visibility="{Binding IsFromServer, Converter={StaticResource BooleanToVisibilityConverter}}">
     <TextBlock Text="{Binding Time}" FontSize="10" Foreground="Gray" Margin="4,0,0,2"/><Border Background="{DynamicResource MaterialDesignCardBackground}" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1" CornerRadius="8" Padding="10,6"><StackPanel><WrapPanel><TextBlock Text="{Binding TypeHex, StringFormat='类型: 0x{0}'}" FontSize="10" Foreground="Gray"/><TextBlock Text="{Binding Label}" FontSize="10" Foreground="Gray" Margin="8,0,0,0"/></WrapPanel><TextBlock Text="{Binding Content}" TextWrapping="Wrap" MaxWidth="360"/></StackPanel></Border>
    </StackPanel>
    <StackPanel Grid.Column="1" HorizontalAlignment="Right" Visibility="{Binding IsFromServer, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
     <TextBlock Text="{Binding Time}" FontSize="10" Foreground="Gray" HorizontalAlignment="Right"/><Border Background="{DynamicResource MaterialDesign.Brush.Primary}" CornerRadius="8" Padding="10,6"><StackPanel><WrapPanel HorizontalAlignment="Right"><TextBlock Text="{Binding Label}" FontSize="10" Foreground="White" Opacity="0.7" Margin="0,0,8,0"/><TextBlock Text="{Binding TypeHex, StringFormat='类型: 0x{0}'}" FontSize="10" Foreground="White" Opacity="0.7"/></WrapPanel><TextBlock Text="{Binding Content}" TextWrapping="Wrap" MaxWidth="360" Foreground="White"/></StackPanel></Border>
    </StackPanel>
   </Grid></DataTemplate></ListBox.ItemTemplate>
  </ListBox>
  <Border Grid.Row="2" Background="{DynamicResource MaterialDesignCardBackground}" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,1,0,0" Padding="8">
   <StackPanel><WrapPanel Margin="0,0,0,8"><TextBlock Text="透传类型(HEX):" VerticalAlignment="Center" Margin="0,0,4,0"/><TextBox Text="{Binding PassthroughTypeHex, UpdateSourceTrigger=PropertyChanged}" Width="48" Margin="0,0,16,0"/><TextBlock Text="发送编码:" VerticalAlignment="Center" Margin="0,0,4,0"/><ComboBox SelectedIndex="{Binding PassthroughEncodingIndex}" Width="85" Margin="0,0,16,0"><ComboBoxItem Content="GBK"/><ComboBoxItem Content="UTF-8"/><ComboBoxItem Content="HEX"/></ComboBox><TextBlock Text="HEX 模式下字符长度必须为偶数" Foreground="Red" FontWeight="Bold" VerticalAlignment="Center" Visibility="{Binding IsHexInputInvalid, Converter={StaticResource BooleanToVisibilityConverter}}"/></WrapPanel>
    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBox Text="{Binding PassthroughInputText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignOutlinedTextBox}" MinHeight="56" MaxHeight="120" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" materialDesign:HintAssist.Hint="输入透传内容..." Margin="0,0,8,0"/><Button Grid.Column="1" Command="{Binding SendPassthroughCommand}" Width="100" MinHeight="56" Style="{StaticResource MaterialDesignRaisedButton}" Foreground="White"><StackPanel Orientation="Horizontal"><materialDesign:PackIcon Kind="Send" Width="18" Height="18" Margin="0,0,8,0"/><TextBlock Text="发送" FontWeight="Bold"/></StackPanel></Button></Grid>
   </StackPanel>
  </Border>
 </Grid>
</UserControl>
