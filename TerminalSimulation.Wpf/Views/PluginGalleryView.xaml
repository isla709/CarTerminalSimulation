<UserControl x:Class="TerminalSimulation.Wpf.Views.PluginGalleryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <ScrollViewer Margin="8" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <ItemsControl ItemsSource="{Binding MainPlugins}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <materialDesign:Card Margin="0,0,0,12" Padding="16">
                        <StackPanel>
                            <WrapPanel Margin="0,0,0,4">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="18" Foreground="{DynamicResource MaterialDesign.Brush.Primary}" />
                                <TextBlock Text="{Binding Version}" Margin="8,4,0,0" Foreground="Gray" FontSize="12" />
                            </WrapPanel>
                            <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Margin="0,0,0,12" Foreground="Gray" FontSize="14" />
                            <ContentControl Content="{Binding Converter={StaticResource PluginToContentConverter}}" HorizontalContentAlignment="Stretch" />
                        </StackPanel>
                    </materialDesign:Card>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</UserControl>
