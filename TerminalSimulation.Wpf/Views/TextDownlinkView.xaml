<UserControl x:Class="TerminalSimulation.Wpf.Views.TextDownlinkView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
 <Grid Background="{DynamicResource MaterialDesignPaper}">
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
  <Border Grid.Row="0" Background="{DynamicResource MaterialDesignCardBackground}" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1" Padding="12">
   <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
    <CheckBox IsChecked="{Binding EnableTTSPlayback}" Content="启用语音播放 (TTS)" Margin="0,0,24,8"/>
    <TextBlock Text="语音声音风格:" VerticalAlignment="Center" Margin="0,0,8,8"/>
    <ComboBox ItemsSource="{Binding InstalledTTSVoices}" SelectedItem="{Binding SelectedTTSVoice}" Width="250" Margin="0,0,24,8" IsEnabled="{Binding EnableTTSPlayback}"/>
    <TextBlock Text="文本解析编码:" VerticalAlignment="Center" Margin="0,0,8,8"/>
    <ComboBox SelectedIndex="{Binding TextDownlinkEncodingIndex}" Width="100" Margin="0,0,16,8"><ComboBoxItem Content="UTF-8"/><ComboBoxItem Content="GBK"/></ComboBox>
   </WrapPanel>
  </Border>
  <ListBox Grid.Row="1" ItemsSource="{Binding TextDownlinkMessages}" Margin="12" Background="Transparent" BorderThickness="0"
           VirtualizingStackPanel.IsVirtualizing="True" VirtualizingStackPanel.VirtualizationMode="Recycling" ScrollViewer.CanContentScroll="True">
   <ListBox.ItemTemplate><DataTemplate>
    <Grid Margin="0,4,0,8"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
     <Border Grid.Column="0" Background="{DynamicResource MaterialDesignCardBackground}" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1" CornerRadius="8" Padding="12" MaxWidth="640" HorizontalAlignment="Left">
      <StackPanel><Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
       <TextBlock Text="{Binding Time}" FontSize="11" Foreground="Gray"/>
       <Border Grid.Column="1" Background="{DynamicResource MaterialDesign.Brush.Primary.Light}" CornerRadius="4" Padding="6,2" Margin="10,0,0,0" HorizontalAlignment="Left"><TextBlock Text="{Binding FlagsDescription}" FontSize="10" Foreground="White" FontWeight="Bold"/></Border>
      </Grid>
      <TextBox Text="{Binding Content}" IsReadOnly="True" BorderThickness="0" Background="Transparent" FontFamily="Consolas" FontSize="14" TextWrapping="Wrap" AcceptsReturn="True" materialDesign:TextFieldAssist.DecorationVisibility="Hidden" Margin="0,0,0,6"/>
      <Expander Header="标志位详情" FontSize="11"><TextBox Text="{Binding DetailedFlagsText}" IsReadOnly="True" BorderThickness="0" Background="Transparent" FontFamily="Consolas" FontSize="11" TextWrapping="Wrap" AcceptsReturn="True" Margin="8,4,0,0" materialDesign:TextFieldAssist.DecorationVisibility="Hidden"/></Expander>
      </StackPanel>
     </Border>
     <Button Grid.Column="1" VerticalAlignment="Top" Margin="8,2,0,0" Style="{StaticResource MaterialDesignIconButton}" ToolTip="重播TTS"
             Command="{Binding DataContext.ReplayTTSCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}"><materialDesign:PackIcon Kind="VolumeHigh" Width="20" Height="20"/></Button>
    </Grid>
   </DataTemplate></ListBox.ItemTemplate>
  </ListBox>
 </Grid>
</UserControl>
