# 车载定位终端模拟系统 (JT808) - Demo 第一版

欢迎使用**车载定位终端模拟系统 (JT808)**。本项目是一个基于 Windows Presentation Foundation (WPF) 开发的现代化模拟终端软件，旨在模拟真实的 JT808 协议车载定位终端设备与后端服务器进行通信。

> **当前版本：Demo 第一版**
> 该版本包含了核心的终端注册、鉴权、位置汇报、数据透传以及地图路径模拟等核心功能，且搭载了全面定制化的现代 UI 体验。

---

## 🚀 核心功能特性

### 1. 协议通信与连接支持
- **多版本协议**：支持 **JT808-2013** 与 **JT808-2019** 双协议版本切换。
- **自定义连接**：可配置服务器 IP、端口、终端手机号以及鉴权码。
- **基础指令**：支持标准的**终端注册**与**终端鉴权**流程。
- **心跳保活 (0x0002)**：支持自定义心跳发送间隔，也可随时关闭自动心跳。

### 2. 详细的位置汇报 (0x0200)
- **手动上报与自动循迹**：支持单次手动发送位置汇报，也支持设定时间间隔进行循环发送。
- **动态坐标模拟**：实时指定经度、纬度、速度、方向（0-359度）及高程数据。
- **位标志配置**：图形化勾选设置各项**报警标志**与**车辆状态标志**。

### 3. 高级功能面板
- **通信日志**：实时呈现终端与服务器之间的上下行报文记录，支持自动滚动和一键清空。
- **数据透传**：支持多种编码格式（GBK、UTF-8、HEX）的数据透传发送与接收，类似聊天的气泡界面，直观区分服务端与终端消息。
- **配置导入/导出**：一键将当前的终端参数及设定项导出为 JSON 文件，并在需要时快速导入恢复。

### 4. 交互式地图与路径模拟 (基于 Leaflet + WebView2)
- **内置地图视图**：内嵌高德地图，可直接在地图上选点来获取经纬度。
- **地图风格切换**：提供多种地图滤镜风格（如午夜深蓝、灰度模式等）。
- **动态小车航向**：小车图标会根据输入的 `Direction` 角度参数进行实时自适应旋转。
- **路径规划及样式**：支持多路径规划显示，可切换如“青蓝霓虹”、“紫粉赛博”等充满科技感的动态路径渲染样式。

### 5. 极致现代化的 UI 界面
- **材质设计 (Material Design)**：全系统采用 MaterialDesignInXamlToolkit，拥有丝滑的点击水波纹、悬浮卡片阴影等现代视觉体验。
- **深浅模式与主题色**：支持明亮与暗黑模式一键切换，自带丰富的系统级主题颜色选择。
- **半透明玻璃与亚克力效果**：支持插入自定义背景壁纸，并自由开启普通半透明、毛玻璃 (Blur) 和亚克力 (Acrylic) 模糊效果，同时可调节板块的透明度，打造独属于你的酷炫工作台。

---

## 🛠️ 技术栈

- **框架**: .NET 8.0 WPF
- **UI 库**: MaterialDesignThemes (Material Design in XAML)
- **网络通信**: 基于底层 Socket 通信实现
- **地图引擎**: WebView2 + HTML5/JS (Leaflet.js + 高德地图服务)
- **架构**: MVVM (依赖注入, CommunityToolkit.Mvvm)

---

## 📦 运行说明

1. 请确保您的系统中安装了 **.NET 8.0 Desktop Runtime**。
2. 确保系统中安装了 **Microsoft Edge WebView2 Runtime**（现代 Windows 系统大多已内置）。
3. 使用 Visual Studio 或 `dotnet run` 编译并运行主项目 `TerminalSimulation.Wpf.csproj`。
4. 程序会在首次运行时自动创建 `Themes` 目录和 `config.json`，并将默认的资源释放其中。

---

## 💡 后续开发计划 (Roadmap)
- 增加对更多 JT808 指令的支持（如多媒体上传、参数设置等）。
- 完善各类报警指令的联合触发测试。

